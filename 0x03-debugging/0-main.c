#include "main.h"

/**
 * main - Test fuction that prints if integer is positive or negative
 * Return: Returns 0 always
 */

int main(void)
{
	int i;

	i = 0;
	positive_or_negative(i);

	return (0);
}
